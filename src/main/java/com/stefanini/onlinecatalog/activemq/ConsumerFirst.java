/*
package com.stefanini.onlinecatalog.activemq;

import javax.annotation.Resource;
import javax.ejb.ActivationConfigProperty;
import javax.ejb.MessageDriven;
import javax.ejb.MessageDrivenContext;
import javax.jms.*;

@MessageDriven( description = "descriere", activationConfig = {
        @ActivationConfigProperty(propertyName = "test" , propertyValue = "java:/ConnectionFactory")
})

public class ConsumerFirst implements  javax.jms.MessageListener {

    @Resource
    private MessageDrivenContext messageDrivenContext;

    public void onMessage(Message message) {

            System.out.println("\n\n\n\n\n==================onMessage=======================!\n\n");
            */
/*if (message instanceof TextMessage) {
                TextMessage msg = (TextMessage) message;
                msg.getText();
            }*//*


    }
}*/
